server.port=8080
spring.application.name=chemistry-backend

# Set one of these for Firebase Admin token verification:
# firebase.service-account.path=C:/path/to/firebase-service-account.json
# firebase.service-account.json={"type":"service_account",...}

# Comma-separated frontend origins allowed to call /api/**.
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,https://milagjurovska.github.io
